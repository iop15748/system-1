package ecust.system.controller;import ecust.system.core.manager.ProcessSchedulingManager;import org.springframework.web.bind.annotation.RequestBody;import org.springframework.web.bind.annotation.RequestMapping;import org.springframework.web.bind.annotation.RequestMethod;import org.springframework.web.bind.annotation.RestController;@RestController@RequestMapping("api/v1")public class ProcessController {	// 单例	private ProcessSchedulingManager manager = ProcessSchedulingManager.getProcessSchedulingManager();	@RequestMapping(value = "/algorithm", method = RequestMethod.PUT, produces = "application/json")	public String setMessage(@RequestBody String algorithm) {		if(manager.isRunning()) {			manager.setMessage(algorithm);			return "success";		}		return "stop";	}}