package ecust.system.core.algorithm;import ecust.system.core.metadata.PCB;import java.util.ArrayList;import java.util.Comparator;public class RR implements ProcessSchedulingAlgorithm {	private ArrayList<PCB> pcbList;	private int timeSliceSize;	private int timeSliceNow;	private int nowPCBIndex;	@Override	public void initialize(ArrayList<PCB> list) {		timeSliceSize = 10;		timeSliceNow = timeSliceSize;		nowPCBIndex = 0;		pcbList = list;		pcbList.sort(Comparator.comparingInt(PCB::getCreateTime));	}	@Override	public void schedule(int currentTime) {		if(timeSliceNow-- > 0) { // 当前时间片未结束			//		} else { // 当前时间片已结束			timeSliceNow = timeSliceSize;		}	}	@Override	public void disuse() {	}}